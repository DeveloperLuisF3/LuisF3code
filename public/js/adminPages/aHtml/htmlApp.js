// firestore DB

// variables
/* let addImage = document.getElementById('addImage');

// initialize app
let initializeHtmlApp = () => {
	addImage.addEventListener('click', downloadImg, false);
	$('#alertWithoutImg').hide();
	$('#alertLoadImg').hide();
};

let downloadImg = () => {
	let file = ($(`.img`))[1].files[0];
	if (!file) {
		$('#alertWithoutImg').show(2000);
		$('#alertWithoutImg').hide(4000);
	} else {
		console.log(file);
		$('#alertLoadImg').show(2000);
		$('#alertLoadImg').hide(4000);
		$("input").val("");
	};
};

window.addEventListener(`load`, initializeHtmlApp, false); */
